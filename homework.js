let q;
let x;
let fruit;
fruit = "apple";

function add(x, y) {
  return x + y;
}

function subtract(x, y) {
  return x - y;
}

function numberToString(n) {
  return String(n);
}

function stringToNumber(s) {
  return Number(s);
}

let arr3 = [1, 2, 3];

let b = true;
